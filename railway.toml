[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn restaurante_project.wsgi --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/"
healthcheckTimeout = 100
